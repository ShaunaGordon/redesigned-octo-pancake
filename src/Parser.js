const Parser = {
    'parseStream': (stream) => {
        // Stream of CSV
        // Street Address, City, Postal Code

        // Make sure at least street address and city (city or postal code?) are present before sending to API

        // Return (yield?) list of objects
    }
};

module.exports = Parser;
