/**
 * Notes: In a more sophisticated application, we might have multiple different ways to output data. In a web application, this class might be the View in an MVC structure, or even the template file for the output page.
 */

const connector = '->';

const Output = {
    invalid: (data) => {
        // address -> Invalid Address
    },

    valid: (data) => {
        // address -> suggestion
    }
}

export default Output;
